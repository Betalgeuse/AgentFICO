# AgentFICO - Milestone M4: Frontend Dashboard

## Overview
| Field | Value |
|:---|:---|
| **Milestone ID** | M4-frontend-dashboard |
| **Title** | Frontend Dashboard UI |
| **Status** | 🚀 In Progress |
| **Start Date** | 2026-01-29 |
| **Target Date** | 2026-02-01 |
| **Priority** | HIGH |
| **Linear Label** | `milestone:M4` |
| **Linear Team** | `web3` |

## Goals
- AgentFICO 점수를 시각적으로 볼 수 있는 대시보드 구현
- 에이전트 주소로 점수 조회 기능
- 점수 상세 분석 (3가지 지표) 시각화
- 리스크 레벨 색상 코딩
- 샘플 에이전트 목록 제공

## Non-Goals
- 사용자 인증/로그인 (MVP 범위 외)
- 모바일 반응형 (추후)
- 다크 모드 (추후)
- i18n 국제화 (추후)

## Tech Stack

| Category | Technology | 용도 |
|:---|:---|:---|
| **Framework** | Vite + React 18 | 빌드 및 개발 |
| **Language** | TypeScript | 타입 안정성 |
| **Styling** | TailwindCSS | 유틸리티 CSS |
| **State** | React Query | API 상태 관리 |
| **Charts** | Recharts | 차트 시각화 |
| **Icons** | Lucide React | 아이콘 |
| **HTTP** | Axios | API 호출 |

## Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Frontend Architecture                         │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                      Dashboard Page                       │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │   │
│  │  │ AgentSearch │  │ ScoreCard   │  │ SampleAgents    │   │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│                              ▼                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    ScoreCard Components                   │   │
│  │  ┌───────────┐  ┌──────────────┐  ┌─────────────────┐   │   │
│  │  │ScoreGauge │  │ScoreBreakdown│  │   RiskBadge     │   │   │
│  │  │ (원형)    │  │ (막대차트)   │  │   (색상뱃지)   │   │   │
│  │  └───────────┘  └──────────────┘  └─────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│                              ▼                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                     API Layer                             │   │
│  │  useScore() hook  →  api/client.ts  →  FastAPI Backend   │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## Directory Structure

```
frontend/
├── src/
│   ├── components/
│   │   ├── ScoreCard.tsx        # 점수 카드 (메인)
│   │   ├── ScoreGauge.tsx       # 원형 게이지
│   │   ├── RiskBadge.tsx        # 리스크 레벨 뱃지
│   │   ├── ScoreBreakdown.tsx   # 3지표 막대 차트
│   │   ├── AgentSearch.tsx      # 주소 검색 입력
│   │   └── SampleAgents.tsx     # 샘플 에이전트 목록
│   ├── pages/
│   │   └── Dashboard.tsx        # 메인 대시보드
│   ├── hooks/
│   │   └── useScore.ts          # 점수 조회 React Query 훅
│   ├── api/
│   │   └── client.ts            # Axios API 클라이언트
│   ├── types/
│   │   └── score.ts             # TypeScript 타입 정의
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css                # TailwindCSS
├── public/
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── postcss.config.js
└── tsconfig.json
```

## Phase 1: 프로젝트 설정

### TASK-001: Vite 프로젝트 초기화
```yaml
task_id: M4-001
title: "Vite + React + TypeScript 프로젝트 초기화"
droid: vite-react-developer
priority: critical
depends_on: []

instructions: |
  ## 목표
  Vite 기반 React TypeScript 프로젝트 생성
  
  ## 명령어
  cd /Users/zayden/Documents/web3_folder/AgentFICO
  npm create vite@latest frontend -- --template react-ts
  cd frontend
  npm install
  
  ## 의존성 설치
  npm install @tanstack/react-query axios recharts lucide-react
  npm install -D tailwindcss postcss autoprefixer
  npx tailwindcss init -p

deliverables:
  - frontend/ 디렉토리
  - package.json
  - vite.config.ts

acceptance_criteria:
  - [ ] npm run dev 실행 성공
  - [ ] TypeScript 컴파일 성공
```

### TASK-002: TailwindCSS 설정
```yaml
task_id: M4-002
title: "TailwindCSS 설정"
droid: vite-react-developer
priority: high
depends_on: [M4-001]

instructions: |
  ## tailwind.config.js
  content 경로 설정
  
  ## src/index.css
  @tailwind base;
  @tailwind components;
  @tailwind utilities;
  
  ## 색상 테마
  - Primary: Indigo
  - Risk Excellent: Green
  - Risk Good: Blue
  - Risk Average: Yellow
  - Risk Below Average: Orange
  - Risk Poor: Red

deliverables:
  - tailwind.config.js
  - src/index.css

acceptance_criteria:
  - [ ] TailwindCSS 클래스 적용 확인
```

## Phase 2: 핵심 컴포넌트

### TASK-003: 타입 정의 및 API 클라이언트
```yaml
task_id: M4-003
title: "타입 정의 및 API 클라이언트"
droid: vite-react-developer
priority: critical
depends_on: [M4-002]

instructions: |
  ## src/types/score.ts
  AgentScore 인터페이스 정의
  - agentAddress: string
  - overall: number (0-1000)
  - txSuccess: number (0-100)
  - x402Profitability: number (0-100)
  - erc8004Stability: number (0-100)
  - riskLevel: number (1-5)
  - riskLevelName: string
  - confidence: number (0-100)
  - timestamp: string
  
  ## src/api/client.ts
  - getScore(address): 점수 조회
  - getContractScore(address): 컨트랙트에서 점수 조회
  - getStats(): 통계 조회
  
  ## src/hooks/useScore.ts
  React Query 기반 useScore 훅

deliverables:
  - src/types/score.ts
  - src/api/client.ts
  - src/hooks/useScore.ts

acceptance_criteria:
  - [ ] 타입 정의 완료
  - [ ] API 호출 성공
```

### TASK-004: ScoreGauge 컴포넌트
```yaml
task_id: M4-004
title: "점수 게이지 컴포넌트"
droid: vite-react-developer
priority: high
depends_on: [M4-003]

instructions: |
  ## 목표
  원형 게이지로 점수 시각화
  
  ## Props
  - value: number (0-1000)
  - max: number (1000)
  - size: number (기본 200)
  
  ## 색상
  - 850+: green-500
  - 750-849: blue-500
  - 650-749: yellow-500
  - 550-649: orange-500
  - <550: red-500
  
  ## 디자인
  - 반원 또는 원형 게이지
  - 중앙에 점수 숫자
  - 부드러운 그라데이션

deliverables:
  - src/components/ScoreGauge.tsx

acceptance_criteria:
  - [ ] 점수 범위별 색상 변경
  - [ ] 애니메이션 효과
```

### TASK-005: ScoreCard 컴포넌트
```yaml
task_id: M4-005
title: "점수 카드 컴포넌트"
droid: vite-react-developer
priority: high
depends_on: [M4-004]

instructions: |
  ## 목표
  전체 점수 정보를 카드 형태로 표시
  
  ## 레이아웃
  1. 헤더: "AgentFICO Score" + RiskBadge
  2. 게이지: ScoreGauge (overall)
  3. 상세: ScoreBreakdown
  4. 푸터: Confidence, Timestamp
  
  ## 스타일
  - 흰색 배경, 둥근 모서리
  - 그림자 효과
  - 반응형 너비

deliverables:
  - src/components/ScoreCard.tsx

acceptance_criteria:
  - [ ] 모든 점수 정보 표시
  - [ ] 깔끔한 레이아웃
```

### TASK-006: ScoreBreakdown 컴포넌트
```yaml
task_id: M4-006
title: "점수 상세 분석 컴포넌트"
droid: vite-react-developer
priority: high
depends_on: [M4-003]

instructions: |
  ## 목표
  3가지 지표를 막대 차트로 시각화
  
  ## 지표
  1. TX Success Rate (40%)
  2. x402 Profitability (40%)
  3. ERC-8004 Stability (20%)
  
  ## Recharts 사용
  BarChart 또는 HorizontalBarChart
  
  ## 표시 정보
  - 지표명
  - 점수 (0-100)
  - 가중치 (%)
  - 막대 색상

deliverables:
  - src/components/ScoreBreakdown.tsx

acceptance_criteria:
  - [ ] 3가지 지표 시각화
  - [ ] 가중치 표시
```

## Phase 3: 페이지 및 통합

### TASK-007: Dashboard 메인 페이지
```yaml
task_id: M4-007
title: "Dashboard 메인 페이지"
droid: vite-react-developer
priority: critical
depends_on: [M4-005, M4-006]

instructions: |
  ## 레이아웃
  1. Header: AgentFICO 로고, 설명
  2. Search: 에이전트 주소 입력
  3. Result: ScoreCard (결과 있을 때)
  4. Samples: 샘플 에이전트 목록
  
  ## 상태 관리
  - 검색 주소 state
  - useScore 훅으로 데이터 조회
  - 로딩, 에러 상태 처리
  
  ## 스타일
  - 전체 배경: gray-100
  - 헤더: indigo-600

deliverables:
  - src/pages/Dashboard.tsx
  - src/App.tsx 업데이트

acceptance_criteria:
  - [ ] 검색 → 결과 표시
  - [ ] 로딩 상태 표시
  - [ ] 에러 처리
```

### TASK-008: 샘플 에이전트 목록
```yaml
task_id: M4-008
title: "샘플 에이전트 목록"
droid: vite-react-developer
priority: medium
depends_on: [M4-007]

instructions: |
  ## 목표
  테스트용 샘플 에이전트 버튼 목록
  
  ## 샘플 에이전트
  - Excellent Trader (0x1111...)
  - Good Analyst (0x2222...)
  - Average Bot (0x3333...)
  - Struggling Agent (0x4444...)
  - Risky Agent (0x5555...)
  
  ## 동작
  클릭 시 해당 주소로 검색

deliverables:
  - src/components/SampleAgents.tsx

acceptance_criteria:
  - [ ] 클릭 시 검색 실행
  - [ ] 티어별 색상 표시
```

### TASK-009: API 연동 테스트
```yaml
task_id: M4-009
title: "API 연동 테스트"
droid: vite-react-developer
priority: high
depends_on: [M4-008]

instructions: |
  ## 테스트 시나리오
  1. Anvil 로컬 노드 실행
  2. FastAPI 서버 실행 (uvicorn)
  3. Frontend 개발 서버 실행
  4. 샘플 에이전트 검색 → 점수 표시
  
  ## CORS 설정
  FastAPI에 CORS 이미 설정됨 (allow_origins=["*"])
  
  ## 환경 변수
  VITE_API_URL=http://localhost:8000

deliverables:
  - 연동 테스트 완료
  - 스크린샷 또는 데모

acceptance_criteria:
  - [ ] API 호출 성공
  - [ ] 점수 정상 표시
  - [ ] 에러 핸들링 동작
```

## Droid Assignment

| Task | Droid | Reason |
|:---|:---|:---|
| M4-001 | vite-react-developer | React 프로젝트 설정 |
| M4-002 | vite-react-developer | TailwindCSS 설정 |
| M4-003 | vite-react-developer | 타입/API 설계 |
| M4-004 | vite-react-developer | UI 컴포넌트 |
| M4-005 | vite-react-developer | UI 컴포넌트 |
| M4-006 | vite-react-developer | 차트 컴포넌트 |
| M4-007 | vite-react-developer | 페이지 구현 |
| M4-008 | vite-react-developer | UI 컴포넌트 |
| M4-009 | vite-react-developer | 통합 테스트 |

## Timeline

| Day | Tasks | Milestone |
|:---|:---|:---|
| Day 1 | M4-001 ~ M4-003 | 프로젝트 설정, 타입/API |
| Day 2 | M4-004 ~ M4-006 | 핵심 컴포넌트 |
| Day 3 | M4-007 ~ M4-009 | 페이지 통합, 테스트 |

## Success Criteria

- [ ] Vite 프로젝트 정상 빌드
- [ ] 모든 컴포넌트 구현
- [ ] API 연동 성공
- [ ] 점수 시각화 정상 동작
- [ ] 샘플 에이전트 테스트 완료

## UI Preview (예상)

```
┌────────────────────────────────────────────────┐
│  🏦 AgentFICO                                  │
│  AI Agent Credit Scoring                       │
├────────────────────────────────────────────────┤
│                                                │
│  [Enter agent address... ] [Search]            │
│                                                │
│  ┌──────────────────────────────────────────┐  │
│  │  AgentFICO Score           [EXCELLENT]   │  │
│  │                                          │  │
│  │            ╭───────╮                     │  │
│  │           ╱   860   ╲                    │  │
│  │          ╱           ╲                   │  │
│  │         ▕             ▏                  │  │
│  │                                          │  │
│  │  TX Success      ████████████░░  90%     │  │
│  │  x402 Profit     ████████████░░  85%     │  │
│  │  ERC-8004        ████████░░░░░░  80%     │  │
│  │                                          │  │
│  │  Confidence: 95%                         │  │
│  └──────────────────────────────────────────┘  │
│                                                │
│  Sample Agents:                                │
│  [Excellent] [Good] [Average] [Poor]           │
│                                                │
└────────────────────────────────────────────────┘
```

## Phase 4: ERC-8004 에이전트 레지스트리 연동

### TASK-010: 백엔드 ERC-8004 Identity Registry 클라이언트
```yaml
task_id: M4-010
title: "ERC-8004 Identity Registry 온체인 조회"
droid: web3-api-developer
priority: high
depends_on: [M4-009]

instructions: |
  ## 목표
  ERC-8004 Identity Registry 컨트랙트에서 에이전트 목록 조회
  
  ## 컨트랙트 주소
  - Sepolia: 0xf66e7CBdAE1Cb710fee7732E4e1f173624e137A7
  - Base Sepolia: 0xdc527768082c489e0ee228d24d3cfa290214f387
  
  ## 구현 (api/src/data_sources/erc8004_registry.py)
  - get_total_agents(): 전체 에이전트 수
  - get_agent_metadata(agent_id): 에이전트 메타데이터 조회
  - list_agents(limit, offset): 에이전트 목록 페이지네이션
  - get_agent_by_address(address): 주소로 에이전트 조회
  
  ## ERC-721 함수 사용
  - totalSupply(): 전체 토큰 수
  - tokenURI(tokenId): 메타데이터 URI
  - ownerOf(tokenId): 소유자 주소

deliverables:
  - api/src/data_sources/erc8004_registry.py
  - 단위 테스트

acceptance_criteria:
  - [ ] Sepolia/Base Sepolia 컨트랙트 조회 성공
  - [ ] 에이전트 메타데이터 파싱
  - [ ] 캐싱 구현 (1분)
```

### TASK-011: 에이전트 목록 API 엔드포인트
```yaml
task_id: M4-011
title: "에이전트 목록 REST API"
droid: fastapi-pro
priority: high
depends_on: [M4-010]

instructions: |
  ## 엔드포인트
  GET /v1/agents
  - Query params: chain (sepolia|base-sepolia), limit, offset
  - Response: { agents: [...], total: number, hasMore: boolean }
  
  GET /v1/agents/{agent_id}
  - Path param: agent_id (ERC-721 tokenId)
  - Query param: chain
  - Response: Agent 상세 정보
  
  ## 응답 스키마
  ```json
  {
    "agentId": 123,
    "name": "DeFi Lending Pro",
    "address": "0x...",
    "chain": "base-sepolia",
    "endpoint": "MCP",
    "score": 21.8,
    "owner": "0x...",
    "hasX402": true,
    "createdAt": "2026-01-29T12:00:00Z"
  }
  ```

deliverables:
  - api/src/routes/agents.py
  - 단위 테스트

acceptance_criteria:
  - [ ] GET /v1/agents 동작
  - [ ] 페이지네이션 정상
  - [ ] 캐싱 동작
```

### TASK-012: 프론트엔드 실시간 에이전트 목록
```yaml
task_id: M4-012
title: "실시간 에이전트 목록 연동"
droid: vite-react-developer
priority: high
depends_on: [M4-011]

instructions: |
  ## 기존 컴포넌트 수정
  - AgentList.tsx: 하드코딩 데이터 → API 연동
  - useAgents() 훅 추가
  
  ## 기능
  - 체인 필터 (All, Sepolia, Base Sepolia)
  - 무한 스크롤 또는 페이지네이션
  - 실시간 새로고침 버튼
  - 로딩/에러 상태

deliverables:
  - src/hooks/useAgents.ts
  - src/components/AgentList.tsx 업데이트

acceptance_criteria:
  - [ ] API에서 에이전트 목록 조회
  - [ ] 체인 필터 동작
  - [ ] 페이지네이션 동작
```

## ERC-8004 Registry 정보

### 공식 배포 주소 (ChaosChain Reference Implementation)

| Network | Chain ID | Identity Registry | Status |
|---------|----------|-------------------|--------|
| **Sepolia** | 11155111 | `0xf66e7CBdAE1Cb710fee7732E4e1f173624e137A7` | ✅ Verified |
| **Base Sepolia** | 84532 | `0xdc527768082c489e0ee228d24d3cfa290214f387` | ✅ Deployed |

### Identity Registry ABI (핵심 함수)
```solidity
// ERC-721 기반
function totalSupply() external view returns (uint256);
function tokenURI(uint256 tokenId) external view returns (string memory);
function ownerOf(uint256 tokenId) external view returns (address);

// ERC-8004 확장
function getAgentWallet(uint256 agentId) external view returns (address);
function getMetadata(uint256 agentId, string memory key) external view returns (bytes memory);

// Events
event Registered(uint256 indexed agentId, string agentURI, address indexed owner);
```

### Agent Registration File 스키마
```json
{
  "type": "https://eips.ethereum.org/EIPS/eip-8004#registration-v1",
  "name": "Agent Name",
  "description": "Agent description",
  "image": "https://...",
  "services": [
    { "name": "MCP", "endpoint": "https://..." },
    { "name": "A2A", "endpoint": "https://..." }
  ],
  "x402Support": true,
  "active": true
}
```

## References

- [Vite Documentation](https://vitejs.dev)
- [TailwindCSS](https://tailwindcss.com)
- [React Query](https://tanstack.com/query)
- [Recharts](https://recharts.org)
- [API Endpoints](../../api/src/routes/)
- [ERC-8004 Specification](https://eips.ethereum.org/EIPS/eip-8004)
- [ERC-8004 Reference Implementation](https://github.com/ChaosChain/trustless-agents-erc-ri)
