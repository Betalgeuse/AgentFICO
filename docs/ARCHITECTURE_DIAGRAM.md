# AgentFICO Ecosystem Architecture

**Last Updated:** 2026-01-30

---

## 1. 전체 시스템 개요

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              AgentFICO Ecosystem                                 │
│                                                                                  │
│   "AI 에이전트를 위한 신용 평가 인프라"                                          │
│                                                                                  │
│   ┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐               │
│   │   AgentFICO     │   │ AgentFICO-Agents│   │ AgentFICO-Config│               │
│   │   (인프라)      │   │ (활용 에이전트) │   │ (Private 설정)  │               │
│   │   Public Repo   │   │   Public Repo   │   │  Private Repo   │               │
│   └─────────────────┘   └─────────────────┘   └─────────────────┘               │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. AgentFICO (인프라 레포)

**역할:** 에이전트 신용 점수 계산 및 온체인 저장

```
AgentFICO/
├── api/                     # Python FastAPI 서버
│   ├── src/
│   │   ├── data_sources/    # 데이터 수집
│   │   │   ├── etherscan.py       # 트랜잭션 히스토리 (✅ Active)
│   │   │   ├── x402_nodata.py     # x402 프로토콜 (⚠️ Placeholder)
│   │   │   └── erc8004_nodata.py  # ERC-8004 레지스트리 (⚠️ Placeholder)
│   │   ├── services/
│   │   │   ├── score_calculator.py  # 점수 계산 로직
│   │   │   ├── contract_client.py   # 스마트 컨트랙트 연동
│   │   │   └── anti_gaming/         # Anti-Gaming 시스템
│   │   │       ├── config_loader.py # AgentFICO-Config 로드
│   │   │       ├── time_decay.py    # 시간 기반 가중치
│   │   │       └── anomaly.py       # 이상 탐지
│   │   └── routers/
│   │       ├── score.py       # /v1/score/{address}
│   │       └── contract.py    # /v1/contract/stats
│   └── requirements.txt
│
├── contracts/               # Solidity (Foundry)
│   ├── src/
│   │   ├── AgentFICOScore.sol     # V1 (deprecated)
│   │   └── AgentFICOScoreV2.sol   # V2 UUPS Proxy
│   ├── script/
│   │   ├── DeployV2.s.sol         # 초기 배포
│   │   └── UpgradeV2.s.sol        # 업그레이드
│   └── test/
│       ├── AgentFICOScore.t.sol   # V1 테스트 (46개)
│       └── AgentFICOScoreV2.t.sol # V2 테스트 (54개)
│
├── frontend/                # React + Vite + TailwindCSS
│   └── src/
│       ├── components/
│       │   ├── Dashboard.tsx
│       │   └── RealAgentList.tsx
│       └── services/
│           └── api.ts
│
├── webhook/                 # Telegram 알림 서비스
│   ├── main.py
│   └── requirements.txt
│
└── docs/
    ├── DEPLOYMENT_STATUS.md
    ├── SECURITY_ANALYSIS.md
    └── adr/                  # Architecture Decision Records
```

---

## 3. AgentFICO-Agents (활용 에이전트 레포)

**역할:** AgentFICO 점수를 활용하는 레퍼런스 에이전트들

```
AgentFICO-Agents/
├── agents/
│   ├── info/                      # 📊 정보 서비스 (중립)
│   │   ├── portfolio-scout/       # 에이전트 발견/추천
│   │   │   ├── src/
│   │   │   │   ├── index.ts       # Hono 라우터
│   │   │   │   └── agentfico.ts   # AgentFICO API 클라이언트
│   │   │   └── agent.json         # ERC-8004 manifest
│   │   └── risk-monitor/          # 위험 모니터링
│   │       └── src/
│   │           └── index.ts
│   │
│   └── trading/                   # 💰 투자 실행 (수익 추구)
│       ├── strategy-replicator/   # 복사 거래
│       └── arbitrage-hunter/      # 차익 거래
│
├── server/                        # 통합 API 서버 (Hono)
│   └── src/
│       ├── index.ts               # 라우터 마운트
│       └── agents/
│           ├── portfolio-scout.ts
│           └── risk-monitor.ts
│
├── packages/
│   └── agentfico-client/          # 공통 클라이언트 라이브러리
│
├── contracts/                     # 에이전트 자체 컨트랙트
└── dashboard/                     # 에이전트 대시보드
```

---

## 4. AgentFICO-Config (Private 레포)

**역할:** Anti-Gaming 계수 (비공개로 유지하여 게이밍 방지)

```
AgentFICO-Config/
├── coefficients/
│   ├── time_decay.json      # 시간 기반 가중치 감소
│   ├── anomaly.json         # 이상 탐지 임계값
│   ├── consistency.json     # 일관성 보너스 설정
│   ├── tx_quality.json      # 트랜잭션 품질 기준
│   └── sybil.json           # 시빌 공격 탐지 (비활성화)
│
├── .env.production          # 프로덕션 환경 변수
├── .env.staging             # 스테이징 환경 변수
└── README.md
```

---

## 5. 데이터 흐름 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              사용자 및 외부 에이전트                              │
└─────────────────────────────────────────────────────────────────────────────────┘
        │                                              │
        │ HTTP                                         │ x402 결제
        ▼                                              ▼
┌───────────────────────┐                    ┌───────────────────────┐
│  AgentFICO Dashboard  │                    │  AgentFICO-Agents     │
│  (Vercel)             │                    │  (Render)             │
│                       │                    │                       │
│  agentfico.luerre.ai  │                    │  agentfico-agents.    │
│                       │                    │  onrender.com         │
│  - React + TailwindCSS│                    │                       │
│  - 점수 시각화        │                    │  - Portfolio Scout    │
│  - 에이전트 목록      │                    │  - Risk Monitor       │
└───────────────────────┘                    │  - Strategy Replicator│
        │                                    │  - Arbitrage Hunter   │
        │                                    └───────────────────────┘
        │                                              │
        │ API 호출                                     │ AgentFICO API 호출
        ▼                                              ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           AgentFICO API Server                                   │
│                           (로컬 PM2 - 외부 접근 필요)                            │
│                                                                                  │
│   localhost:8000                                                                 │
│                                                                                  │
│   엔드포인트:                                                                    │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │  GET  /v1/score/{address}   → 점수 계산 (실시간)                        │   │
│   │  GET  /v1/contract/stats    → 컨트랙트 통계                             │   │
│   │  GET  /v1/agents            → ERC-8004 에이전트 목록                    │   │
│   │  GET  /health               → 상태 확인                                 │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│   점수 계산 공식:                                                                │
│   overall = (txSuccess × 0.4 + x402 × 0.4 + erc8004 × 0.2) × 10                 │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
        │                     │                     │
        │ Etherscan API       │ (미래)              │ 온체인 조회
        ▼                     ▼                     ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────────────────────────────┐
│  Etherscan    │    │  x402 Protocol│    │  AgentFICOScoreV2 Contract            │
│               │    │  (Not Ready)  │    │  (Base Sepolia)                       │
│  트랜잭션     │    │               │    │                                       │
│  히스토리     │    │  수익률 데이터│    │  0xdF7699A597662330E553C0f48CEb16a... │
│               │    │               │    │                                       │
│  ✅ Active    │    │  ⚠️ Placeholder│    │  - 4 agents registered                │
└───────────────┘    └───────────────┘    │  - UUPS Upgradeable                   │
                                          │  - Hybrid Update Model                │
                                          └───────────────────────────────────────┘
                                                          │
                                                          │ 이벤트 발생
                                                          ▼
                                          ┌───────────────────────────────────────┐
                                          │  Event Listener (TODO)                │
                                          │  - ScoreUpdated 감지                  │
                                          │  - ScoreQueried 감지                  │
                                          └───────────────────────────────────────┘
                                                          │
                                                          │ HTTP POST
                                                          ▼
                                          ┌───────────────────────────────────────┐
                                          │  Telegram Webhook                     │
                                          │  (Render)                             │
                                          │                                       │
                                          │  agentfico-webhook.onrender.com       │
                                          │                                       │
                                          │  엔드포인트:                          │
                                          │  - POST /webhook/score-updated        │
                                          │  - POST /webhook/score-queried        │
                                          │  - POST /webhook/generic              │
                                          └───────────────────────────────────────┘
                                                          │
                                                          │ Telegram API
                                                          ▼
                                          ┌───────────────────────────────────────┐
                                          │  Telegram Bot (@AgentFICOBot)         │
                                          │  → 관리자 알림                        │
                                          └───────────────────────────────────────┘
```

---

## 6. 배포 현황

### 6.1 라이브 서비스

| 서비스 | URL | 플랫폼 | 상태 |
|--------|-----|--------|------|
| **Dashboard** | `agentfico.luerre.ai` | Vercel | ✅ |
| **Webhook** | `agentfico-webhook.onrender.com` | Render | ✅ |
| **Agents API** | `agentfico-agents.onrender.com` | Render | ✅ |
| **Contract** | `0xdF7699...c6` | Base Sepolia | ✅ |
| **Score API** | `localhost:8000` | Local PM2 | ⚠️ 로컬만 |

### 6.2 스마트 컨트랙트

| 네트워크 | Proxy Address | Implementation |
|----------|---------------|----------------|
| **Base Sepolia** | `0xdF7699A597662330E553C0f48CEb16ace8b339C6` | `0x92e4FAF37DaD2f3BF300D550732f24fB76A63020` |

**주요 함수:**
```solidity
// 점수 업데이트 (owner only)
function updateScore(address agent, uint256 overall, ...) external onlyOwner

// 배치 업데이트
function batchUpdateScores(address[] agents, uint256[] overalls, ...) external onlyOwner

// 점수 조회 (+ 이벤트 발생)
function getScore(address agent) external returns (AgentScore memory)
function getScoreOnly(address agent) external returns (uint256)

// 리스크 평가
function assessRisk(address agent) external view returns (uint8)

// 유료 업데이트 요청 (1시간 쿨다운)
function requestScoreUpdate() external payable
```

**이벤트:**
```solidity
event ScoreUpdated(address indexed agent, uint256 overall, uint8 riskLevel, ...);
event ScoreQueried(address indexed agent, address indexed queriedBy, uint256 overall);
```

---

## 7. AgentFICO-Agents 상세

### 7.1 에이전트 목록

| 에이전트 | 유형 | 기능 | 엔드포인트 |
|----------|------|------|------------|
| **Portfolio Scout** | Info | 고점수 에이전트 발견/추천 | `/portfolio-scout/get-top-agents` |
| **Risk Monitor** | Info | 보유 에이전트 위험 알림 | `/risk-monitor/check-risk` |
| **Strategy Replicator** | Trading | 고점수 에이전트 전략 복사 | `/strategy-replicator/replicate` |
| **Arbitrage Hunter** | Trading | 점수 기반 차익거래 기회 | `/arbitrage-hunter/find-opportunities` |

### 7.2 프로토콜 지원

```
┌─────────────────────────────────────────────────────────────┐
│                    ERC-8004 Ecosystem                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  [Other Agents] ──x402 결제──▶ [AgentFICO-Agents]           │
│                                     │                        │
│                                     ▼                        │
│                            AgentFICO API 호출                │
│                                     │                        │
│                                     ▼                        │
│                           서비스 결과 반환                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

| 프로토콜 | 용도 | 상태 |
|----------|------|------|
| **ERC-8004** | 온체인 에이전트 ID | ✅ |
| **x402** | Agent-to-Agent 결제 | ✅ |
| **A2A** | Agent-to-Agent 통신 | ✅ |

### 7.3 수익 모델

| 에이전트 | B2B (Agent-to-Agent) | B2C (Human) |
|----------|---------------------|-------------|
| Portfolio Scout | x402 API 호출 수수료 | 월 구독 |
| Risk Monitor | 위험 콜백 수수료 | 알림 서비스 |
| Strategy Replicator | 복사 수수료 쉐어 | 플랫폼 수수료 |
| Arbitrage Hunter | 기회 데이터 판매 | 자체 거래 수익 |

---

## 8. Anti-Gaming 시스템

AgentFICO-Config (Private Repo)에서 계수를 로드하여 점수 조작을 방지합니다.

```
┌─────────────────────────────────────────────────────────────┐
│                    Score Calculator                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  Anti-Gaming Pipeline                        │
│                                                              │
│   1. Time Decay        - 최근 활동에 더 높은 가중치          │
│   2. Anomaly Detection - 비정상 패턴 탐지 및 페널티          │
│   3. Consistency Bonus - 꾸준한 활동에 보너스                │
│   4. TX Quality        - 트랜잭션 품질 평가                  │
│   5. Sybil Detection   - 시빌 공격 탐지 (비활성화)           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              AgentFICO-Config (Private)                      │
│                                                              │
│   coefficients/                                              │
│   ├── time_decay.json    ← 실제 가중치 값 (비공개)          │
│   ├── anomaly.json       ← 탐지 임계값 (비공개)             │
│   ├── consistency.json   ← 보너스 기준 (비공개)             │
│   ├── tx_quality.json    ← 품질 기준 (비공개)               │
│   └── sybil.json         ← 시빌 탐지 설정                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**로드 우선순위:**
1. 환경변수 (`AG_CONFIG_*`) - 프로덕션 배포용
2. 파일 시스템 (`/etc/agentfico/config`) - 컨테이너 마운트
3. 로컬 경로 (`../AgentFICO-Config`) - 개발용
4. 기본값 (Fallback) - 실제 값과 다름

---

## 9. 해결해야 할 사항

| 우선순위 | 항목 | 상태 |
|----------|------|------|
| 🔴 **P0** | API 외부 접근 설정 | ❌ 미완료 |
| 🟡 **P1** | Contract Event Listener | ❌ 미완료 |
| 🟡 **P1** | ERC-8004 데이터 활성화 | ❌ 미완료 |
| 🟡 **P1** | Private Config 프로덕션 배포 | ❌ 미완료 |
| 🟢 **P2** | 데이터베이스 캐싱 | ❌ 미완료 |
| 🟢 **P2** | HTTPS 설정 (API) | ❌ 미완료 |

---

## 10. 관련 링크

| 리소스 | URL |
|--------|-----|
| **AgentFICO GitHub** | https://github.com/Betalgeuse/AgentFICO |
| **AgentFICO-Agents GitHub** | https://github.com/Betalgeuse/AgentFICO-Agents |
| **Dashboard** | https://agentfico.luerre.ai |
| **Agents API** | https://agentfico-agents.onrender.com |
| **Webhook** | https://agentfico-webhook.onrender.com |
| **Contract (Basescan)** | https://sepolia.basescan.org/address/0xdF7699A597662330E553C0f48CEb16ace8b339C6 |

---

*Generated by AgentFICO Development Team*
